require('dotenv').config()
const { resolve } = require('path');

module.exports = {
    config: resolve('config/db.ts'),
    'seeders-path': resolve('seeders'),
    'migrations-path': resolve('migrations'),
    'models-path': resolve('models')
};
